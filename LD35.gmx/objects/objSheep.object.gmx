<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<object>
  <spriteName>sprSheepStatic</spriteName>
  <solid>0</solid>
  <visible>-1</visible>
  <depth>0</depth>
  <persistent>0</persistent>
  <parentName>objCanCollide</parentName>
  <maskName>&lt;undefined&gt;</maskName>
  <events>
    <event eventtype="0" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Setup default variables

//Speed variables
baseSpeed = room_speed / 60;

maxSpeed = room_speed / 20;

maxAvoid = room_speed / 40;


//Determines size of local flock
distMult = 1.5;

//Radius around sheep at which they are scared of dog
dogMult = 2;
scareDist = sprite_width * dogMult;

//Sets initial direction
currentDir = irandom(360);
changedSpriteDir = 999;

//Currently not used, could add randomness to sheep's movement
meanderDir = random_range(-5,5);
alarm[0] = floor(random(2) * room_speed);

scared = false;
nearDog = false;

//Chance of getting scared if near a scared sheep/unscared if not. Per tick.
scareChance = 0.6;
unscareChance = 0.3;

//For movement code 3, still used in wander
targetX = x;
targetY = y;


//speedDist = scareDist;

wandering = false;


//eShell = 5;

//wanderSpeed = baseSpeed;
//Limit of # seconds between wanders of a single idle sheep
wanderTime = 40;

var rTime = random(wanderTime);
alarm[1] = floor(rTime * room_speed);

//For physics/vector movement
steeringX = phy_speed_x;
steeringY = phy_speed_y;

//What is considered "close" in some steering code
closeDist = 50;

//For whether scared of a bark
barkScared = false;
barkDist = scareDist * 2.2;
nearBark = false;


//Maybe allow scaling movement (slows down as sheep gets further from Dog)
//Attempted it and it caused issues, may go back and try different method
slowingRadius = scareDist * 0.2;
barkSlowRadius = barkDist * 0.2;



//For avoiding walls (not implemented yet)
wallAvoiding = false;
wallTargetX = phy_position_x;
wallTargetY = phy_position_y;

//To keep sheep from moving for 1 second after they become not scared
//Prevents them walking towards the dog and back a lot
unscaredTimer = 999;
timeIdleAfterScared = 1 * room_speed;
</string>
          </argument>
        </arguments>
      </action>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Sprite array


numSprites = 16;

spriteArray[15] = sprSheepESE;
spriteArray[14] = sprSheepSE;
spriteArray[13] = sprSheepSSE;
spriteArray[12] = sprSheepRunSouth;
spriteArray[11] = sprSheepSSW;
spriteArray[10] = sprSheepSW;
spriteArray[9] = sprSheepWSW;
spriteArray[8] = sprSheepRunWest;
spriteArray[7] = sprSheepWNW;
spriteArray[6] = sprSheepNW;
spriteArray[5] = sprSheepNNW;
spriteArray[4] = sprSheepRunNorth;
spriteArray[3] = sprSheepNNE;
spriteArray[2] = sprSheepNE;
spriteArray[1] = sprSheepENE;
spriteArray[0] = sprSheepRunEast;



</string>
          </argument>
        </arguments>
      </action>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>/// Create shadow
myShadow = instance_create(x, y+50, objShadow)
myShadow.parent = self.id;
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="1" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>/// Destroy shadow
with(myShadow) {
  instance_destroy();
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="2" enumb="1">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Turn on wander

if (!scared) {
  wandering = true;
  targetX = x + random_range(-75, 75);
  targetY = y + random_range(-75, 75);
  
  phy_speed_x = baseSpeed * random_range(-1, 1);
  phy_speed_y = baseSpeed * random_range(-1, 1);
}
var rTime = random(wanderTime);
alarm[1] = floor(rTime * room_speed);
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="2" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Change meander direction

meanderDir = random_range(-10,10);

alarm[0] = floor(random(2) * room_speed);
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="3" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///REMOVED: New herding code
/*
var sheep = self.id;
var findScared = false;

with (objSheep) {  
  var dist = scrEucDist(sheep, id);
  var dir = point_direction(sheep.x, sheep.y, x, y);
  
    if ((id != sheep.id) &amp;&amp; (dist &lt; global.maxDist) &amp;&amp; (abs(dir - sheep.direction) &lt; 90)) {
        if (nearDog) {          
          //if (random(1) &lt; scareChance) {
            sheep.scared = true;
          //}
          findScared = true;
        }
    }  
}

if (!findScared &amp;&amp; random(1) &lt; unscareChance) {
  sheep.scared = false;
}


if (scared) {

  /////////////Movement based on local flock/////////////
  var centerx = 0;
  var centery = 0;
  var flockCount = 0;
  var cenDir;
  
  var directions = 0;
  var speeds = 0;
  var moveCount = 0;
  
  with (objSheep) {
    //Only consider if within maxDist of main sheep, and is not same sheep
    var dist = scrEucDist(sheep, id);
    var dir = point_direction(sheep.x, sheep.y, x, y);
    
    //Based on center of flock
    if ((id != sheep.id) &amp;&amp; (dist &lt; global.maxDist)) {// &amp;&amp; (abs(dir - sheep.direction) &lt; 90)) {
      centerx += x;
      centery += y;
      flockCount++;      
    }  
    
    //This is a little weird, because some are affected by old direction and some by new
    //Come back to this if we have time and see if we can figure this out without making
    //it too time consuming
    //Based on other sheep within local flock that are moving
    if (speed &gt; 0) {
      directions += direction;
      speeds += speed;
      moveCount++;
    }
  }
  
  if (flockCount &gt; 0) {
    centerx /= flockCount;
    centery /= flockCount;
    cenDir = point_direction(x, y, centerx, centery);
  }
  
  if (moveCount &gt; 0) {
    directions /= moveCount;
    speeds /= moveCount;
  }
  
  //Try to avoid colliding with other sheep
  var cirColl = collision_circle(x, y, (sprite_width * distMult), objSheep, false, true);
  
  var collDir;
  
  if (cirColl != noone) {
    distMult = random(1) + 1;
    collDir = point_direction(cirColl.x, cirColl.y, x, y);
  }
  
  var trigger = false;
  if (flockCount &gt; 0) {
    trigger = true;
    if (moveCount &gt; 0) {
      if (cirColl != noone) {
        direction = (cenDir + directions + collDir) / 3;
      } else {
        direction = (cenDir + directions) / 2;
      }
    } else if (cirColl != noone) {
      direction = (cenDir + collDir) / 2;
    } else {
      direction = cenDir;
    }
  } else if (cirColl != noone) {
    direction = collDir;
    trigger = true;
  }
  
  
  var coll = instance_place(x+hspeed, y + vspeed, objCanCollide);

  if (coll != noone) {
    direction = point_direction(coll.x, coll.y, x, y);
    speed = baseSpeed;
    //speed = 0;
    trigger = true;
  }
  
  ////////////////////////////////////////////////////////////////////////////////////
  /////////////Movement based on dog/////////////
  var dog = instance_find(objDog, 0);
  
  //Where the sheep predicts the dog shall be
  var dogDist = scrEucDist(self.id, dog);
  var updatesAhead = dogDist / maxSpeed;
  var dX = (dog.x - dog.xprevious) * updatesAhead + dog.x;
  var dY = (dog.y - dog.yprevious) * updatesAhead + dog.y;
  
  //Get direction to run from
  var runDir = point_direction(dX, dY, x, y);
  
  //Figure out how fast to run and how much dog should influence their direction
  var scareMult = 1 - dogDist / scareDist;
  
  if (scareMult &lt; 0.1) {
    scareMult = 0.1;
  }  
  
  
  flockMult = 1 - scareMult;
  
  
  
  
  if (trigger) {
    direction = (flockMult * direction + scareMult * runDir);
  } else {
    direction = runDir;
  }
  speed = maxSpeed * scareMult;
  if (speed &lt; baseSpeed) {
    speed = baseSpeed;
  }
  scared = true;

} else {
  speed = 0;
}

*/
</string>
          </argument>
        </arguments>
      </action>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///REMOVED - Determine direction and move
/*

if (scared) {
  var temp = scrBasicFlock(self.id);
  
  //Go to the center of the sheep near this sheep, but try to avoid each other
  if (temp[0] &gt;= 0 || temp[1] = 0) {
    direction = point_direction(x, y, temp[0], temp[1]);
    speed = baseSpeed;
  } else {
    speed = 0;
  }
  
  //Don't actually collide with each other
  var coll = instance_place(x+hspeed, y + vspeed, objCanCollide);
  
  if (coll != noone) {
    direction = point_direction(coll.x, coll.y, x, y);
    speed = baseSpeed;
    //speed = 0;
  }
  
  /*if (place_meeting(x+hspeed,y,objCanCollide)) {
    hspeed=0;
  }
  if (place_meeting(x,y+vspeed,objCanCollide)) {
    vspeed=0;
  }*/
/*  
  } else {
  speed = 0;  
}

var dogColl = collision_circle(x, y, scareDist, objDog, false, true);

if (dogColl != noone) {
  
  //Where the sheep predicts the dog shall be
  var dogDist = scrEucDist(self.id, dogColl);
  var updatesAhead = dogDist / maxSpeed;
  var dX = (dogColl.x - dogColl.xprevious) * updatesAhead + dogColl.x;
  var dY = (dogColl.y - dogColl.yprevious) * updatesAhead + dogColl.y;
  
  //Get direction to run from
  var runDir = point_direction(dX, dY, x, y);
  
  //Figure out how fast to run and how much dog should influence their direction
  var scareMult = 1 - dogDist / scareDist;
  
  if (scareMult &lt; 0) {
    scareMult = 0;
  } else if (scareMult &lt; 0.1) {
    scareMult = 0.1;
  }  
  
  
  flockMult = 1 - scareMult;
    
  direction = (flockMult * direction + scareMult * runDir);
  
  speed = maxSpeed * scareMult;
  scared = true;
  direction += wanderDir;
} else {
  
  with (objSheep) {
    var dist = scrEucDist(other.id, id);
    if (dist &lt; global.maxDist) {
      if (scared){ 
        other.scared = true;
      } 
    }
  }

  scared = false;
}


*/
</string>
          </argument>
        </arguments>
      </action>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///REMOVED:Flock Herding/Herd Flocking code attempt 3
/*var sheep = self.id;
var findScared = false;

with (objSheep) {  
  var dist = scrEucDist(sheep, id);
  var dir = point_direction(sheep.x, sheep.y, x, y);
  
    if ((id != sheep.id) &amp;&amp; (dist &lt; global.maxDist) &amp;&amp; (abs(dir - sheep.direction) &lt; 90)) {
        if (nearDog) {          
          if (random(1) &lt; scareChance) {
            sheep.scared = true;
          }
          findScared = true;
        }
    }  
}

if (!findScared &amp;&amp; random(1) &lt; unscareChance) {
  sheep.scared = false;
}


if (scared) {

  /////////////Movement based on local flock/////////////
  var centerX = 0;
  var centerY = 0;
  var flockCount = 0;
  
  var moveX = 0;
  var moveY = 0;
  var moveCount = 0;
  
  with (objSheep) {
    //Only consider if within maxDist of main sheep, and is not same sheep
    var dist = scrEucDist(sheep, id);
    var dir = point_direction(sheep.x, sheep.y, x, y);
    
    //Based on center of flock
    if ((id != sheep.id) &amp;&amp; (dist &lt; global.maxDist)) {// &amp;&amp; (abs(dir - sheep.direction) &lt; 90)) {
      centerX += x;
      centerY += y;
      flockCount++;      
    }  
    
    if (targetX != x || targetY != y) {
      moveX += targetX;
      moveY += targetY;
      moveCount++;
    }
  }
  
  if (flockCount &gt; 0) {
    centerX /= flockCount;
    centerY /= flockCount;
  }
  
  if (moveCount &gt; 0) {
    moveX /= moveCount;
    moveY /= moveCount;
  }
  
  //Try to avoid colliding with other sheep
  var cirColl = collision_circle(x, y, (sprite_width * distMult), objSheep, false, true);
  
  var collX;
  var collY;
  
  if (cirColl != noone) {
    distMult = random(1) + 1;
    collDir = point_direction(cirColl.x, cirColl.y, x, y);
    collDist = point_distance(cirColl.x, cirColl.y, x, y);
    collX = lengthdir_x(collDist, collDir) + x;
    collY = lengthdir_y(collDist, collDir) + y;
  }
  
  var trigger = false;
  if (flockCount &gt; 0) {
    trigger = true;
    if (moveCount &gt; 0) {
      if (cirColl != noone) {
        dX = (centerX + moveX + collX) / 3;
        dY = (centerY + moveY + collY) / 3;
      } else {
        dX = (centerX + moveX) / 2;
        dY = (centerY + moveY) / 2;
      }
    } else if (cirColl != noone) {
      dX = (centerX + collX) / 2;
      dY = (centerY + collY) / 2;      
    } else {
      dX = centerX;
      dY = centerY;
    }
  } else if (cirColl != noone) {
    dX = collX;
    dY = collY;
    trigger = true;
  } else {
    dX = x;
    dY = y;
  }
  

if (collision_line(x, y, dX, dY, objCanCollide, false, true)) {
  dX += random_range(-50, 50);
  dY += random_range(-50, 50);
}
  //direction = point_direction(dX, dY, x, y);
  
  ////////////////////////////////////////////////////////////////////////////////////
  /////////////Movement based on dog/////////////
  var dog = instance_find(objDog, 0);
  
  //Where the sheep predicts the dog shall be
  var dogDist = scrEucDist(self.id, dog);
  var updatesAhead = dogDist / maxSpeed;
  var dogFutureX = (dog.x - dog.xprevious) * updatesAhead + dog.x;
  var dogFutureY = (dog.y - dog.yprevious) * updatesAhead + dog.y;
  
  //x &amp; y dist from dog
  var dogXDist = x - dog.x;
  var dogYDist = y - dog.y; 

  var ratio = dogDist / scareDist;
  
  dogTargetX = x + (dogXDist * 1/ratio);
  dogTargetY = y + (dogYDist * 1/ratio);
  
  //Figure out how fast to run and how much dog should influence their direction
  var scareMult = 1 - ratio;  
  if (scareMult &lt; 0.1) {
    scareMult = 0.1;
  }      
  flockMult = 1 - scareMult;
  
  
  
  
  if (trigger) {
    targetX = (flockMult * dX) + (scareMult * dogTargetX);
    targetY = (flockMult * dY) + (scareMult * dogTargetY);
  } else {
    targetX = dogTargetX;
    targetY = dogTargetY;
  }
    
  scared = true;

} else {
  if (wandering) {
    
    speed = wanderSpeed;
    if (point_distance(x, y, targetX, targetY) &lt; eShell ) {
      wandering = false;
      var rTime = random(wanderTime);
      alarm[1] = floor(rTime * room_speed);
      speed = 0;
    }
  } else {
    speed = 0;
  }
}


*/
</string>
          </argument>
        </arguments>
      </action>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///REMOVED: Move based on target x &amp; y
/*direction = point_direction(x, y, targetX, targetY);
var dist = point_distance(x, y, targetX, targetY);
speed = dist/speedDist * baseSpeed;
direction += meanderDir;

var coll = instance_place(x+hspeed, y + vspeed, objCanCollide);

if (coll != noone) {
//  direction = point_direction(coll.x, coll.y, x, y);
//  speed = baseSpeed;
  speed = 0;
  //trigger = true;
}

*/
</string>
          </argument>
        </arguments>
      </action>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Physics-based/vector-based steering behaviors

var sheep = self.id;
var findScared = false;

/*var numNear = 0;
var nearSheep;*/



//Determine if scared
with (objSheep) {  
  var dist = scrEucDist(sheep, id);
  var dir = point_direction(sheep.x, sheep.y, x, y);
  
    if ((id != sheep.id) &amp;&amp; (dist &lt; global.maxDist)) {// &amp;&amp; (abs(dir - sheep.direction) &lt; 90)) {
        
        if (nearDog || nearBark) {          
          if (random(1) &lt; scareChance) {
            sheep.scared = true;
          }
          findScared = true;
        }
    }  
}

if (!findScared &amp;&amp; random(1) &lt; unscareChance) {
  sheep.scared = false;
  sheep.barkScared = false;
  distMult = 1 + random(1);
  unscaredTimer = 0;
  wallAvoiding = false;
}


if (scared) {
  wandering = false
  //Reset steering
  steeringX = 0;
  steeringY = 0;
  
  ////////////////////////Flee dog///////////////////////
  
  var dog = instance_find(objDog, 0);
  
  var dX = phy_position_x - dog.phy_position_x;
  var dY = phy_position_y - dog.phy_position_y;
  var dogDist = scrEucDist(self.id, dog);//point_distance(x, x, dog.x, dog.x);
  
  var desiredVec = scrNormalizeVector(dX, dY);
  fleeSteeringX = (desiredVec[0] * maxSpeed) - phy_speed_x;
  fleeSteeringY = (desiredVec[1] * maxSpeed) - phy_speed_y;


  
  ////////////////////////Flee bark///////////////////////  
  if (barkScared) {
    var bark = instance_find(objDog, 0);
    
    dX = phy_position_x - bark.x;
    dY = phy_position_y - bark.y;
    
    var barkD = scrEucDist(self.id, bark);//point_distance(x, x, dog.x, dog.x);
    
    var desiredVec = scrNormalizeVector(dX, dY);
    barkSteeringX = (desiredVec[0] * maxSpeed * 1.6) - phy_speed_x;
    barkSteeringY = (desiredVec[1] * maxSpeed * 1.6) - phy_speed_y;  
  } else {
    barkSteeringX = 0;
    barkSteeringY = 0;
  }  
  
  
  /////////////////////////////Get info on flock///////////////////////  
  var centerX = 0;
  var centerY = 0;
  var flockCount = 0;
  
  var moveX = 0;
  var moveY = 0;
  var moveCount = 0;
  
  with (objSheep) {
    //Only consider if within maxDist of main sheep, and is not same sheep
    var dist = scrEucDist(sheep, id);
    var dir = point_direction(sheep.x, sheep.y, x, y);
    
    //Getcenter of flock
    if ((id != sheep.id) &amp;&amp; (dist &lt; global.maxDist) &amp;&amp; (abs(dir - sheep.direction) &lt; 90)) {
      centerX += phy_position_x;
      centerY += phy_position_y;
      flockCount++;      
    }  
    
    //Get movement of flock
/*    if (phy_speed_x != 0 || phy_speed_y != 0) {
      show_debug_message(string(phy_speed_x) + ", " + string(phy_speed_y));
      moveX += phy_speed_x;
      moveY += phy_speed_y;
      moveCount++;
    }*/

    /*//Get sheep within small area
    if (dist &lt; sprite_width * distMult) {
      nearSheep[numNear++] = self.id;
    }*/
    
  }
  
  if (flockCount &gt; 0) {
    centerX /= flockCount;
    centerY /= flockCount;
  } else {
    centerX = x;
    centerY = y;
  }
  
  if (moveCount &gt; 0) {
    moveX /= moveCount;
    moveY /= moveCount;
  }
  
  
  /////////////////////////////Steer to center of local flock///////////////////////  
  
  if (flockCount &gt; 1) {
    dX = centerX - phy_position_x;
    dY = centerY - phy_position_y;
    
    
    var desiredVec = scrNormalizeVector(dX, dY);
    centerSteeringX = (desiredVec[0] * maxSpeed * 0.9) - phy_speed_x;
    centerSteeringY = (desiredVec[1] * maxSpeed * 0.9) - phy_speed_y;
  } else {
    centerSteeringX = 0;
    centerSteeringY = 0;
  }
  /////////////////////////////Steer to average move of local flock///////////////////////  
  /*dX = moveX - x;
  dY = moveY - y;
    
  var desiredVec = scrNormalizeVector(dX, dY);
  moveSteeringX = (desiredVec[0] * maxSpeed) - phy_speed_x;
  moveSteeringY = (desiredVec[1] * maxSpeed) - phy_speed_y;
  */
  ////////////////////////////Avoid other sheep//////////////////////////////
  /*var minDist;
  var minIndex;
  if (numNear &gt; 0) {
    minDist = scrEucDist(sheep, nearSheep[0]);
    minIndex = 0;
    for (var i = 1; i &lt; numNear; ++i) {
      var curDist = scrEucDist(sheep, nearSheep[i]);
      if (curDist &lt; minDist) {
        minDist = curDist;
        minIndex = i;
      }
    }
  }
  
  var closest = nearSheep[minIndex];*/
  /*
  var length = point_distance(0, 0, phy_speed_x, phy_speed_y) * 2 / maxSpeed;
  
  var normalizedSpeed = scrNormalizeVector(phy_speed_x, phy_speed_y);
  
  var aheadX = phy_position_x + normalizedSpeed[0] * length;
  var aheadY = phy_position_y + normalizedSpeed[1] * length;
  
  var coll = collision_line(x, y, aheadX, aheadY, objCanCollide, false, true);
  
  if (coll != noone) {
    dX = aheadX - coll.x;
    dY = aheadY - coll.y;
    
    
    desiredVec = scrNormalizeVector(dX, dY);
    avoidanceSteeringX = (desiredVec[0] * maxAvoid) - phy_speed_x;
    avoidanceSteeringY = (desiredVec[1] * maxAvoid) - phy_speed_y;
  } else {*/
   // avoidanceSteeringX = 0;
   // avoidanceSteeringY = 0;
  //}

  
  ///////////////Calculate final///////////////////////////////////////////////
  steeringX = fleeSteeringX + centerSteeringX + barkSteeringX;
  steeringY = fleeSteeringY + centerSteeringY + barkSteeringY;
  phy_speed_x = phy_speed_x + steeringX;
  phy_speed_x = min(abs(phy_speed_x), maxSpeed) * sign(phy_speed_x);
  phy_speed_y = phy_speed_y + steeringY;
  phy_speed_y = min(abs(phy_speed_y), maxSpeed) * sign(phy_speed_y);

  
} else if (!wandering) {
  phy_speed_x = 0;
  phy_speed_y = 0;
}
</string>
          </argument>
        </arguments>
      </action>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Check if near dog or bark

var dogColl = collision_circle(x, y, scareDist, objDog, false, true);

if (dogColl != noone &amp;&amp; !dogColl.sneaking) {
  nearDog = true;
  scared = true;
  wandering = false;
} else {
  nearDog = false;
}

var barkColl = collision_circle(x, y, barkDist, objBark, false, true);

if (barkColl != noone) {
  nearBark = true;
  scared = true;
  barkScared = true;
  wandering = false;
} else {
  nearBark = false;
}
</string>
          </argument>
        </arguments>
      </action>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Drift apart slightly if not scared

if (!scared) {
  if (unscaredTimer &lt; timeIdleAfterScared) {
    var sheep = self.id;
    
    var nearSheep;
    var numNear = 0;
    
    with (objSheep) {
      var dist = scrEucDist(self.id, sheep);
      if (sheep != self.id &amp;&amp; dist &lt; sprite_width * 1.3) {
        nearSheep[numNear++] = self.id;        
      }
    }
    
    fleeSteeringX = 0;
    fleeSteeringY = 0;
    
    if (numNear &gt; 0) {
      for (var i = 0; i &lt; numNear; ++i) {
        var dog = instance_find(objDog, 0);
        
        var dX = x - nearSheep[i].x;
        var dY = y - nearSheep[i].y;
         
        
        var desiredVec = scrNormalizeVector(dX, dY);
        fleeSteeringX += (desiredVec[0] * baseSpeed) - phy_speed_x;
        fleeSteeringY += (desiredVec[1] * baseSpeed) - phy_speed_y;  
      }
    } else {
      phy_speed_x = 0;
      phy_speed_y = 0;
    }
    
    steeringX = fleeSteeringX;
    steeringY = fleeSteeringY;
    phy_speed_x = phy_speed_x + steeringX;
    phy_speed_x = min(abs(phy_speed_x), baseSpeed) * sign(phy_speed_x);
    phy_speed_y = phy_speed_y + steeringY;
    phy_speed_y = min(abs(phy_speed_y), baseSpeed) * sign(phy_speed_y);
  } else {
    unscaredTimer++;
  }
}
</string>
          </argument>
        </arguments>
      </action>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Wandering code

if (wandering &amp;&amp; !scared &amp;&amp; unscaredTimer &lt; timeIdleAfterScared) {

  if (point_distance(x, y, targetX, targetY) &lt; 5) {
    wandering = false;
  }

  dX = targetX - x;
  dY = targetY - y;
  
  
  var desiredVec = scrNormalizeVector(dX, dY);
  steeringX = (desiredVec[0] * baseSpeed * 0.9) - phy_speed_x;
  steeringY = (desiredVec[1] * baseSpeed * 0.9) - phy_speed_y;

  phy_speed_x = phy_speed_x + steeringX;
  phy_speed_x = min(abs(phy_speed_x), baseSpeed) * sign(phy_speed_x);
  phy_speed_y = phy_speed_y + steeringY;
  phy_speed_y = min(abs(phy_speed_y), baseSpeed) * sign(phy_speed_y);

  
}
</string>
          </argument>
        </arguments>
      </action>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Handle if too close to "walls"
//Should work if both scared or not.  
//Should just add a steering force of some power to current speeds
//Should push some direction within the 180 away from the wall

if (!wallAvoiding &amp;&amp; !scared) {

  var xwall = false;
  var ywall = false;
  

  if (phy_position_x &lt; 55) {
    var dir1 = random_range(315, 405) % 360;
    xwall = true;
  } else if (phy_position_x &gt; 730) {
    var dir1 = random_range(135, 225) % 360;
    xwall = true;
  }
  
  

  if (phy_position_y &lt; 60 ) {
    var dir2 = random_range(225, 315);
    dir2 = (dir2 + 180) % 360
    ywall = true;
  } else if (phy_position_y &gt; 470) {
    var dir2 = random_range(45, 135);
    ywall = true; 
  }
  
  if (xwall) {

    wallAvoidance = true;
    var normedX = x + lengthdir_x(sprite_width * 2, dir1);
    var normedY = y + lengthdir_y(sprite_width * 2, dir1);
    
    var dX = normedX - x;
    var dY = normedY - y;
    
    
    var desiredVec = scrNormalizeVector(dX, dY);
    var dir1SteeringX = (desiredVec[0] * baseSpeed) - phy_speed_x;
    var dir1SteeringY = (desiredVec[1] * baseSpeed) - phy_speed_y;
  } else {
    var dir1SteeringX = 0;
    var dir1SteeringY = 0;
   
  }

  
  if (ywall) {
    wallAvoidance = true;
    var normedX2 = x + lengthdir_x(sprite_width * 2, dir2);
    var normedY2 = y + lengthdir_y(sprite_width * 2, dir2);
    
    var dX = normedX2 - phy_position_x;
    var dY = normedY2 - phy_position_y;
    
    
    var desiredVec = scrNormalizeVector(dX, dY);
    var dir2SteeringX = (desiredVec[0] * baseSpeed) - phy_speed_x;
    var dir2SteeringY = (desiredVec[1] * baseSpeed) - phy_speed_y;
  } else {
    var dir2SteeringX = 0;
    var dir2SteeringY = 0;
  }
  
  var steeringX = dir1SteeringX + dir2SteeringX;
  var steeringY = dir2SteeringY + dir2SteeringY;
  
  if (steeringX &gt; 0 || steeringY &gt; 0) {
    wallAvoiding = true;
    if (xwall) {
      wallTargetX = normedX;
      wallTargetY = normedY;
      if (ywall) {
        wallTargetX = (wallTargetX + normedX2) / 2;
        wallTergetY = (wallTargetY + normedY2) / 2;
      }
    } else if (ywall) {
      wallTargetX = normedX2;
      wallTergetY = normedY2;
    } else {
      wallTargetX = phy_position_x;
      wallTargetY = phy_position_y;
    }
  }
  
  phy_speed_x = phy_speed_x + steeringX;
  phy_speed_x = min(abs(phy_speed_x), maxSpeed) * sign(phy_speed_x);
  phy_speed_y = phy_speed_y + steeringY;
  phy_speed_y = min(abs(phy_speed_y), maxSpeed) * sign(phy_speed_y);
  
} else if (!scared) {
  
  var dX = wallTargetX - phy_position_x;
  var dY = wallTargetY - phy_position_y;
  
  
  var desiredVec = scrNormalizeVector(dX, dY);
  var steeringX = (desiredVec[0] * baseSpeed) - phy_speed_x;
  var steeringY = (desiredVec[1] * baseSpeed) - phy_speed_y;
  
  phy_speed_x = phy_speed_x + steeringX;
  phy_speed_x = min(abs(phy_speed_x), maxSpeed) * sign(phy_speed_x);
  phy_speed_y = phy_speed_y + steeringY;
  phy_speed_y = min(abs(phy_speed_y), maxSpeed) * sign(phy_speed_y);
  
  var dist = point_distance(phy_position_x, phy_position_y, wallTargetX, wallTargetY);
  if (dist &lt; 5) {
    wallAvoiding = false;
  }
 
}
</string>
          </argument>
        </arguments>
      </action>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///REDO LATER: Keep sheep on screen

var newx = phy_position_x + phy_speed_x;
var newy = phy_position_y + phy_speed_y;


//if (newy &gt;= 320) {
  if (newx &lt; 40) {
    phy_speed_x = 0;
    phy_position_x = 40;
  } else if (newx &gt; 760) {
    phy_speed_x = 0;
    phy_position_x = 760;
  }
/*} else {e
  calc = newx  * (0.5) + 30; 
  show_debug_message(string(newx) + " @ " + string(calc));
  if (newx &lt; calc) {
    phy_speed_x = 0;
    phy_position_x = calc;
  }
}*/
/* 
if (newx &lt; 0 || newx &gt; room_width) {
  phy_speed_x *= -1;
}
*/

if (newy &lt; 98 ) {
  phy_speed_y = 0;
  phy_position_y = 98
} else if (newy &gt; 445) {
  phy_speed_y = 0;
  phy_position_y = 445;
}
</string>
          </argument>
        </arguments>
      </action>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///REDO: Change sprite direction
/*if (phy_speed_x != 0 || phy_speed_y != 0) {
  image_angle = point_direction(0, 0, phy_speed_x, phy_speed_y) + 180;
  //show_debug_message(string(phy_speed_x) + ", " + string(phy_speed_y) + " = " + string(point_direction(0, 0, phy_speed_x, phy_speed_y)));
}*/

if (phy_speed_x != 0 || phy_speed_y != 0) {
  //If moving
  
    
  //Get proper image index
  if (sprite_index == sprSheepStatic) {
    var image = 0;
  } else {
    var image = image_index;
  }
  
  //Get current direction
  currentDir = point_direction(0, 0, phy_speed_x, phy_speed_y) 
  
  var index = (floor(((currentDir / 11.25) + 1)/2)) % 16;
  var whichSprite = spriteArray[index];
  
  if ((sprite_index != whichSprite) &amp;&amp; (changedSpriteDir &gt; room_speed / 6)) {
    image_speed = 0.5;  
    sprite_index = whichSprite;
    image_index = image;
    changedSpriteDir = 0;
  }
  
} else {
  image_speed = 0;
  //If not moving
  //make sure in the right sprite
  if (sprite_index != sprSheepStatic) {
    sprite_index = sprSheepStatic;
  }
    
  var indexCalc = floor(((currentDir / 11.25) + 1)/2) % 16;
  
  var index = (indexCalc + 3) % 16;
  
  if ((index != image_index) &amp;&amp; (changedSpriteDir &gt; room_speed / 3)) {
    image_index = index;
    changedSpriteDir = 0;
  } 
  
}

  changedSpriteDir++;
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="4" ename="objCanCollide">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Empty
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </events>
  <PhysicsObject>-1</PhysicsObject>
  <PhysicsObjectSensor>0</PhysicsObjectSensor>
  <PhysicsObjectShape>0</PhysicsObjectShape>
  <PhysicsObjectDensity>0.5</PhysicsObjectDensity>
  <PhysicsObjectRestitution>0.100000001490116</PhysicsObjectRestitution>
  <PhysicsObjectGroup>0</PhysicsObjectGroup>
  <PhysicsObjectLinearDamping>0</PhysicsObjectLinearDamping>
  <PhysicsObjectAngularDamping>0</PhysicsObjectAngularDamping>
  <PhysicsObjectFriction>0</PhysicsObjectFriction>
  <PhysicsObjectAwake>-1</PhysicsObjectAwake>
  <PhysicsObjectKinematic>0</PhysicsObjectKinematic>
  <PhysicsShapePoints>
    <point>10,10</point>
    <point>10,10</point>
  </PhysicsShapePoints>
</object>
